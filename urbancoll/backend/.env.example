# For local SQLite quick-start (default):
PROVIDER="sqlite"
DATABASE_URL="file:./dev.db"
JWT_SECRET="change_this_secret"
PORT=4000

# Example Postgres (production) - uncomment and set PROVIDER to 'postgresql'
# PROVIDER="postgresql"
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public"

# Stripe (optional): set your secret key to enable real payments
# STRIPE_SECRET="sk_test_xxx"
